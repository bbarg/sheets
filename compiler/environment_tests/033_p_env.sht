#~ Code before being preprocessed ~#
int result

func int snuggle():
    int[] output[10]

#~ 
 ~ This for loop will assigns values 
 ~ to all the elements of vector 'output'
 ~#

    for (int i=0; i<10 ; i=i+1;):
        if (x == 9):
            output[i] = 1 ## only last elem=1
        else:
            result[i] = 0

    return result